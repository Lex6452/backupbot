# 🤖 Бот для Резервного Копирования Баз Данных

[🇺🇸 English Version](README.md)

Мощный Telegram бот для автоматического резервного копирования баз данных с поддержкой SSH и веб-интерфейсом.

## ✨ Возможности

- **🔄 Поддержка Multiple БД**: PostgreSQL, MySQL, SQLite, MongoDB
- **🔐 SSH Подключения**: Безопасные соединения с удаленными серверами
- **📅 Автоматические Бэкапы**: Ежедневное резервное копирование в 02:00
- **📱 Telegram Интерфейс**: Полный контроль через сообщения Telegram
- **📊 Управление Бэкапами**: Скачивание, организация и управление бэкапами
- **🔒 Безопасность**: Доступ только для администраторов
- **📈 Мониторинг**: Тестирование подключений и мониторинг статуса
- **🌐 Управление SSH Серверами**: Прямое управление серверами через SSH
- **📦 Передача Бэкапов**: Загрузка бэкапов на удаленные серверы

## 🚀 Быстрый Старт

### Предварительные Требования

- Python 3.8+
- Токен Telegram бота
- Учетные данные для ваших баз данных

### Установка

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/Lex6452/backupbot.git
   cd backupbot
   ```

2. **Создайте и активируйте виртуальное окружение (рекомендуется)**
   ```bash
   python -m venv venv
   source venv/bin/activate  # В Windows: venv\Scripts\activate
   ```

3. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте окружение**
   ```bash
   cp .env.example .env
   # Отредактируйте .env с вашими настройками
   ```

5. **Настройте файл .env**
   ```env
   BOT_TOKEN=your_telegram_bot_token_here
   ADMIN_ID=your_telegram_user_id
   BACKUP_DIR=./backups
   TIMEZONE=Europe/Moscow
   ```

6. **Запустите бота**
   ```bash
   python main.py
   ```

## 📋 Как Получить Токен Бота и ID Администратора

### Получение Токена Бота

1. Откройте Telegram и найдите `@BotFather`
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен бота (выглядит как `1234567890:ABCdefGHIjklMNopQRstUVwxyz`)
5. Добавьте его в ваш `.env` файл как `BOT_TOKEN`

### Получение Вашего ID Администратора

1. Откройте Telegram и найдите `@userinfobot`
2. Запустите бота
3. Он покажет ваш ID пользователя
4. Скопируйте ID и добавьте в ваш `.env` файл как `ADMIN_ID`

## ⚙️ Конфигурация

### Переменные Окружения

| Переменная | Описание | По умолчанию | Обязательно |
|------------|----------|--------------|-------------|
| `BOT_TOKEN` | Токен бота Telegram от @BotFather | - | **Да** |
| `ADMIN_ID` | Ваш ID пользователя Telegram | - | **Да** |
| `BACKUP_DIR` | Директория для хранения бэкапов | `./backups` | Нет |
| `TIMEZONE` | Часовой пояс для планировщика | `Europe/Moscow` | Нет |

### Типы Подключений к Базе Данных

#### PostgreSQL
- **Обязательно**: Хост, Порт, База данных, Имя пользователя, Пароль
- **Инструмент бэкапа**: `pg_dump`
- **Тестовая команда**: `psql --version`

#### MySQL
- **Обязательно**: Хост, Порт, База данных, Имя пользователя, Пароль
- **Инструмент бэкапа**: `mysqldump`
- **Тестовая команда**: `mysql --version`

#### SQLite
- **Обязательно**: Путь к файлу
- **Опционально**: Данные SSH подключения
- **Метод бэкапа**: Копирование файла или передача по SSH
- **Тестовая команда**: `sqlite3 --version`

#### MongoDB
- **Обязательно**: Хост, Порт, База данных, Имя пользователя, Пароль
- **Инструмент бэкапа**: `mongodump`
- **Тестовая команда**: `mongodump --version`

## 🗂️ Структура Проекта

```
backupbot/
├── main.py                 # Главная точка входа
├── handlers/               # Обработчики Telegram
│   ├── admin.py           # Команды и меню администратора
│   ├── backup.py          # Управление бэкапами
│   ├── ssh_handlers.py    # Управление SSH серверами
│   └── snapshot_handlers.py # Управление серверами бэкапов
├── utils/                  # Вспомогательные модули
│   ├── db.py              # Операции с базой данных
│   ├── scheduler.py       # Планировщик автоматических бэкапов
│   ├── ssh_client.py      # Управление SSH подключениями
│   ├── ssh_utils.py       # SSH утилиты и ping
│   ├── backup_transfer.py # Передача файлов бэкапов
│   └── connection_test.py # Тестирование подключений к БД
├── backups/                # Директория хранения бэкапов
├── logs/                   # Логи приложения
└── requirements.txt        # Зависимости Python
```

## 🎯 Использование

### Запуск Бота

1. **Запустите приложение бота:**
   ```bash
   python main.py
   ```

2. **Откройте Telegram и найдите вашего бота**
3. **Отправьте команду `/start`**
4. **Вы увидите главное меню со всеми доступными опциями**

### Опции Главного Меню

- **📊 Список подключений** - Просмотр и управление всеми подключениями к БД
- **➕ Добавить подключение** - Добавить новое подключение к БД
- **🔄 Сделать бэкап** - Немедленное создание бэкапа
- **📁 Менеджер бэкапов** - Просмотр, скачивание и управление файлами бэкапов
- **🔐 SSH** - Управление SSH серверами и выполнение команд
- **⚙️ Настройки автобэкапа** - Настройка автоматических расписаний бэкапов
- **📋 Логи бэкапов** - Просмотр истории и статуса бэкапов

### Добавление Подключения к Базе Данных

1. Нажмите **"➕ Добавить подключение"**
2. Выберите тип базы данных:
   - **PostgreSQL** - Для баз данных PostgreSQL
   - **MySQL** - Для баз данных MySQL/MariaDB  
   - **SQLite** - Для файлов баз данных SQLite
   - **MongoDB** - Для баз данных MongoDB

3. **Для PostgreSQL/MySQL/MongoDB:**
   - Введите имя подключения
   - Укажите хост, порт, имя базы данных
   - Введите имя пользователя и пароль
   - Протестируйте подключение перед сохранением

4. **Для SQLite:**
   - Выберите между локальным файлом или SSH подключением
   - Для SSH: Укажите учетные данные SSH и путь к удаленному файлу
   - Для локального: Укажите локальный путь к файлу

### SSH Возможности

Когда `SSH_ENABLED=true` в вашем `.env` файле:

- **🔌 Подключиться** - Установить SSH соединение с серверами
- **🏓 Пинг** - Измерить время отклика сервера с детальной статистикой
- **🔄 Перезагрузить** - Безопасно перезагрузить удаленные серверы с мониторингом
- **📦 Обновить** - Обновить системные пакеты с отслеживанием прогресса
- **💻 Выполнить** - Запускать команды в интерактивной SSH сессии
- **📁 Просмотреть** - Навигация по файловой системе сервера

### Автоматические Бэкапы

- **Расписание**: Запускается ежедневно в 02:00 (настраивается)
- **Область**: Резервное копирование всех включенных подключений
- **Отчетность**: Отправка детального отчета администратору после завершения
- **Логирование**: Полное логирование всех попыток бэкапа
- **Сервер бэкапов**: Опциональная загрузка на удаленные серверы бэкапов

## 🔧 Продвинутые Возможности

### Управление Бэкапами

- **Файловый браузер**: Постраничный список всех файлов бэкапов
- **Прямое скачивание**: Скачивание бэкапов прямо в Telegram
- **Информация о файлах**: Просмотр размеров, дат и времени создания
- **Организация**: Автоматическое именование с временными метками

### Тестирование Подключений

- **Предварительное тестирование**: Тестирование подключений перед сохранением учетных данных
- **Детальные отчеты**: Получение версии БД, размера и статуса
- **Диагностика ошибок**: Четкие сообщения об ошибках для устранения неполадок
- **Проверка SSH**: Тестирование SSH подключений и доступа к файлам

### Функции Безопасности

- **Доступ только для администраторов**: Ограничено настроенным ID администратора
- **Безопасное хранение**: Зашифрованное хранение учетных данных в базе данных
- **Таймауты подключений**: Предотвращение зависающих подключений
- **Валидация ввода**: Очищенные пользовательские вводы
